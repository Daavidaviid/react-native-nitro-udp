///
/// HybridUdpSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ArrayBuffer` to properly resolve imports.
namespace NitroModules { class ArrayBuffer; }

#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include <functional>

namespace margelo::nitro::udp {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Udp`
   * Inherit this class to create instances of `HybridUdpSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridUdp: public HybridUdpSpec {
   * public:
   *   HybridUdp(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridUdpSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridUdpSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridUdpSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual void initialize(const std::string& host, double port) = 0;
      virtual void send(const std::shared_ptr<ArrayBuffer>& data) = 0;
      virtual void onReceive(const std::function<void(const std::shared_ptr<ArrayBuffer>& /* data */)>& callback) = 0;
      virtual void close() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Udp";
  };

} // namespace margelo::nitro::udp
